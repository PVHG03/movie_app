import { Router } from "express";

// import { getRecommendations } from "../controllers/recommendationController";

const router = Router();

// router.get("/:algorithm/:mediaType", getRecommendations);

export default router;